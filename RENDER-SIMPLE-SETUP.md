# Простой деплой на Render БЕЗ Redis и S3

## ✅ Можно обойтись без Redis и S3!

Приложение теперь работает **без Redis и S3**. Эти сервисы опциональны:

- **Без Redis**: Очередь задач (Bull) не будет работать, но все остальное работает
- **Без S3**: Загрузка скриншотов не будет работать, но все остальное работает

## Что делать при создании Blueprint

### Оставьте ВСЕ поля пустыми:

- ✅ REDIS_HOST → **оставить пустым**
- ✅ REDIS_PORT → **оставить пустым**
- ✅ REDIS_PASSWORD → **оставить пустым**
- ✅ MINIO_ENDPOINT → **оставить пустым**
- ✅ MINIO_ACCESS_KEY → **оставить пустым**
- ✅ MINIO_SECRET_KEY → **оставить пустым**
- ✅ SEMRUSH_API_KEY → **оставить пустым** (опционально)

### Просто нажмите "Deploy Blueprint"!

## Что будет работать

✅ **Backend запустится** - все API endpoints работают  
✅ **Frontend запустится** - интерфейс доступен  
✅ **База данных работает** - PostgreSQL подключена  
✅ **Аутентификация работает** - JWT токены  
✅ **Управление устройствами** - создание, просмотр, обновление  
✅ **Управление задачами** - создание, просмотр (но без очереди)  
✅ **Парсинг данных** - сохранение в базу данных  
✅ **WebSocket логи** - real-time подключение  

## Что НЕ будет работать

❌ **Очередь задач (Bull)** - создание задач будет работать, но они не будут автоматически обрабатываться через очередь  
❌ **Загрузка скриншотов** - эндпоинт `/api/agent/screenshot` вернет ошибку  
❌ **Просмотр артефактов** - эндпоинт `/api/artifacts/*` вернет ошибку  

## Когда понадобятся Redis и S3?

### Redis нужен, если:
- Хотите использовать автоматическую очередь задач
- Нужна обработка задач в фоне
- Нужен мониторинг очереди через Bull Board

### S3 нужен, если:
- Нужно загружать скриншоты с Android устройств
- Нужно хранить файлы (артефакты)
- Нужно просматривать скриншоты через веб-интерфейс

## Добавление Redis/S3 позже

Вы можете добавить Redis и S3 в любой момент:

1. Настройте Redis (Upstash) или S3 (DigitalOcean Spaces)
2. В Render Dashboard откройте ваш Backend Service
3. Перейдите в "Environment"
4. Добавьте переменные окружения
5. Render автоматически перезапустит сервис

## Итого

**Сейчас:** Оставьте все поля пустыми → Нажмите "Deploy Blueprint" → Готово! 🎉

**Позже:** Добавьте Redis/S3 когда понадобятся эти функции.

