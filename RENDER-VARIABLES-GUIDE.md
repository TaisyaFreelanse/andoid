# Руководство по заполнению переменных окружения в Render

## Что заполнять при создании Blueprint

### ✅ МОЖНО ОСТАВИТЬ ВСЕ ПУСТЫМ!

**Redis и S3 теперь опциональны!** Приложение работает без них.

### Вариант 1: Простой деплой (рекомендуется для начала)

**Оставьте ВСЕ поля пустыми:**
- REDIS_HOST → пусто
- REDIS_PORT → пусто
- REDIS_PASSWORD → пусто
- MINIO_ENDPOINT → пусто
- MINIO_ACCESS_KEY → пусто
- MINIO_SECRET_KEY → пусто
- SEMRUSH_API_KEY → пусто

**Просто нажмите "Deploy Blueprint"!**

Приложение запустится и будет работать (без очереди задач и загрузки файлов).

### Вариант 2: С временными значениями (если нужны все функции сразу)

Если хотите сразу использовать все функции, заполните:

```
MINIO_ACCESS_KEY = temp
MINIO_SECRET_KEY = temp
```

**Важно:** Это временные значения! После создания сервисов нужно будет:
1. Настроить реальное S3 хранилище (DigitalOcean Spaces, AWS S3 и т.д.)
2. Заменить эти значения на реальные credentials

### ⚠️ Можно оставить пустыми (но лучше заполнить позже):

```
REDIS_HOST = (оставить пустым)
REDIS_PORT = (оставить пустым)
REDIS_PASSWORD = (оставить пустым)
```

**Примечание:** Без Redis очередь задач (Bull) не будет работать, но Backend запустится.

### 📝 Опционально:

```
SEMRUSH_API_KEY = (оставить пустым)
```

**Примечание:** Нужен только если используете Semrush API для проверки доменов.

## После создания Blueprint

### Шаг 1: Настройте Redis (Upstash - бесплатно)

1. Зарегистрируйтесь на [upstash.com](https://upstash.com)
2. Создайте новый Redis database
3. Выберите регион, близкий к вашему Render региону
4. Скопируйте данные:
   - **Endpoint** → это будет `REDIS_HOST`
   - **Port** → это будет `REDIS_PORT` (обычно 6379 или 6380)
   - **Password** → это будет `REDIS_PASSWORD`

5. В Render Dashboard:
   - Откройте ваш Backend Service
   - Перейдите в "Environment"
   - Обновите:
     - `REDIS_HOST` = ваш endpoint
     - `REDIS_PORT` = ваш port
     - `REDIS_PASSWORD` = ваш password
   - Сохраните (Render автоматически перезапустит сервис)

### Шаг 2: Настройте S3 Storage

#### Вариант A: DigitalOcean Spaces (рекомендуется, $5/месяц)

1. Зарегистрируйтесь на [digitalocean.com](https://www.digitalocean.com)
2. Создайте новый Space:
   - **Name**: `android-automation-screenshots`
   - **Region**: Выберите тот же регион, что и Render
   - **CDN**: Включите (опционально)
3. Создайте Access Key:
   - Settings → Spaces Keys → Generate New Key
   - Сохраните Access Key и Secret Key
4. В Render Dashboard:
   - Откройте ваш Backend Service
   - Перейдите в "Environment"
   - Обновите:
     - `MINIO_ENDPOINT` = `<region>.digitaloceanspaces.com` (например: `sfo3.digitaloceanspaces.com`)
     - `MINIO_ACCESS_KEY` = ваш Access Key
     - `MINIO_SECRET_KEY` = ваш Secret Key
   - Сохраните

#### Вариант B: AWS S3

1. Создайте S3 bucket в AWS Console
2. Создайте IAM user с правами на S3
3. Создайте Access Key для этого user
4. В Render Dashboard обновите:
   - `MINIO_ENDPOINT` = `s3.amazonaws.com`
   - `MINIO_ACCESS_KEY` = AWS Access Key
   - `MINIO_SECRET_KEY` = AWS Secret Key

### Шаг 3: Semrush API (опционально)

Если нужна проверка доменов через Semrush:

1. Зарегистрируйтесь на [semrush.com](https://www.semrush.com)
2. Получите API Key
3. В Render Dashboard добавьте:
   - `SEMRUSH_API_KEY` = ваш API key

## Настройка Frontend (ВАЖНО!)

После создания Blueprint, **обязательно** настройте переменную для Static Site:

1. В Render Dashboard откройте Static Site `android-automation-frontend`
2. Перейдите в **Environment**
3. Добавьте переменную:
   - **Key**: `VITE_API_URL`
   - **Value**: `https://android-automation-backend.onrender.com`
4. Сохраните (Render автоматически пересоберет Static Site)

**Без этой переменной frontend будет пытаться делать запросы к своему собственному URL вместо backend!**

Подробнее см. `RENDER-FRONTEND-SETUP.md`

## Проверка работы

После настройки всех переменных:

1. **Backend Health Check:**
   ```
   https://your-backend.onrender.com/health
   ```
   Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend:**
   ```
   https://your-frontend.onrender.com
   ```
   Должен загрузиться интерфейс

3. **Проверка подключения:**
   - Откройте DevTools (F12) → Network
   - Попробуйте залогиниться или открыть страницу Devices
   - Проверьте, что запросы идут на `android-automation-backend.onrender.com`, а не на frontend URL

## Итого: Что делать СЕЙЧАС

**При создании Blueprint заполните только:**

```
MINIO_ACCESS_KEY = temp
MINIO_SECRET_KEY = temp
```

**Все остальное оставьте пустым** и настройте позже через Render Dashboard.

