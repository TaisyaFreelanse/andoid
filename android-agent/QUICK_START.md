# Быстрый старт: Комплексный сценарий парсинга

## 📋 Что нужно сделать

1. **Уникализация устройства** (прокси, гео, таймзона, язык, Android ID, AAID, User-Agent)
2. **Поиск в Google** → "site:pickuplineinsider.com"
3. **Переход на сайт** → клик по ссылке из результатов
4. **Парсинг баннеров** → 5-6 переходов внутри сайта, сбор `addurl` с каждого баннера
5. **Повторение** → новая уникализация и повтор всего процесса

## 🚀 Как использовать

### Шаг 1: Откройте веб-интерфейс
https://android-automation-frontend.onrender.com/

### Шаг 2: Создайте задачу уникализации

1. **Задачи** → **Создать задачу**
2. Заполните:
   - **Название:** "Уникализация #1"
   - **Тип:** `uniqueness`
   - **Устройство:** выберите ваше устройство
   - **Конфигурация (JSON):** скопируйте содержимое файла **`UNIQUENESS_SETUP.json`**
3. Нажмите **"Создать"**
4. Дождитесь статуса **"completed"** ✅

### Шаг 3: Создайте задачу парсинга

1. **Задачи** → **Создать задачу**
2. Заполните:
   - **Название:** "Парсинг баннеров #1"
   - **Тип:** `parsing`
   - **Устройство:** то же устройство
   - **Конфигурация (JSON):** скопируйте содержимое файла **`COMPLEX_SCENARIO.json`**
3. Нажмите **"Создать"**
4. Дождитесь статуса **"completed"** ✅

### Шаг 4: Повторите процесс

После завершения парсинга:
- Создайте новую задачу уникализации
- Затем новую задачу парсинга
- И так далее нужное количество раз

## 📊 Результаты

После выполнения задачи парсинга:

- **В задаче** → раздел **"Результаты"**:
  - `resultJson.parsedData` - все извлеченные данные
  - `resultJson.adUrls` - домены из параметра `addurl` (после дедупликации)

- **В разделе "Parsed Data"**:
  - Все собранные ссылки с баннерами
  - Возможность экспорта в JSON/CSV

### Ожидаемый результат:
- **Страниц:** 6-7 (1 начальная + 5-6 переходов)
- **Баннеров на странице:** 5-8
- **Всего ссылок:** ~30-40
- **Уникальных доменов из addurl:** ~20-30

## ⚙️ Настройка

### Изменить количество переходов:
В `COMPLEX_SCENARIO.json` найдите `"max_iterations": 6` и измените значение.

### Изменить поисковый запрос:
В `COMPLEX_SCENARIO.json` найдите `"value": "site:pickuplineinsider.com"` и измените.

## ⚠️ Требования

- ✅ Root доступен (статус "✓ Доступен")
- ✅ Интернет подключен
- ✅ Прокси настроен (опционально, для подмены гео)

## 📁 Файлы

- **`UNIQUENESS_SETUP.json`** - задача уникализации устройства
- **`COMPLEX_SCENARIO.json`** - задача парсинга баннеров
- **`SCENARIO_INSTRUCTIONS.md`** - подробная инструкция

---

**Готово!** Теперь вы можете запускать комплексный сценарий парсинга.

