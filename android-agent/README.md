# Android Automation Agent

Автономный Android агент для автоматизации браузера, парсинга данных и уникализации устройств.

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Сборка](#сборка)
- [Использование](#использование)
- [Архитектура](#архитектура)
- [API](#api)
- [Сценарии](#сценарии)
- [Troubleshooting](#troubleshooting)

## 📱 Требования

### Системные требования
- **Android**: 7.0+ (API 24+)
- **Root**: Требуется для функций уникализации
- **RAM**: Минимум 2 GB
- **Хранилище**: Минимум 100 MB свободного места

### Для сборки
- **JDK**: 17+
- **Android Studio**: Hedgehog (2023.1.1) или новее
- **Gradle**: 8.2+
- **Kotlin**: 1.9.20+

## 🚀 Установка

### Вариант 1: Установка готового APK

1. Скачайте APK из раздела Releases
2. Включите установку из неизвестных источников:
   - Настройки → Безопасность → Неизвестные источники
3. Установите APK:
   ```bash
   adb install -r automation-agent.apk
   ```

### Вариант 2: Сборка из исходников

```bash
# Клонируйте репозиторий
git clone <repository-url>
cd android-agent

# Соберите debug версию
./gradlew assembleDevDebug

# Или release версию
./gradlew assembleProdRelease

# APK будет в app/build/outputs/apk/
```

### Установка через ADB

```bash
# Установка debug версии
adb install app/build/outputs/apk/dev/debug/app-dev-debug.apk

# Установка release версии
adb install app/build/outputs/apk/prod/release/app-prod-release.apk
```

## ⚙️ Конфигурация

### Настройка Backend URL

Измените `API_BASE_URL` в `app/build.gradle.kts`:

```kotlin
buildConfigField("String", "API_BASE_URL", "\"https://your-api.example.com\"")
```

Или используйте product flavors:
- `dev` - для разработки (localhost)
- `staging` - для тестирования
- `prod` - для production

### Настройка прокси

Прокси настраиваются через backend API или локально в приложении.

Поддерживаемые форматы:
```
socks5://host:port:username:password
http://username:password@host:port
https://host:port
```

### Root доступ

Для функций уникализации требуется root:
1. Установите Magisk или SuperSU
2. Предоставьте root права приложению при первом запросе

## 🔨 Сборка

### Debug сборка

```bash
# Все варианты
./gradlew assembleDebug

# Конкретный flavor
./gradlew assembleDevDebug
./gradlew assembleStagingDebug
./gradlew assembleProdDebug
```

### Release сборка

1. Создайте keystore (см. `keystore/README.md`)
2. Настройте подпись в `build.gradle.kts`
3. Соберите:

```bash
./gradlew assembleProdRelease
```

### Подписанный APK

```bash
# Подпись существующего APK
apksigner sign --ks keystore/release.keystore \
  --ks-key-alias automation-agent \
  --out signed-app.apk \
  app/build/outputs/apk/prod/release/app-prod-release-unsigned.apk
```

## 📖 Использование

### Запуск приложения

1. Откройте приложение
2. Введите URL backend сервера
3. Нажмите "Подключиться"
4. Устройство зарегистрируется на backend

### Основные функции

#### 1. Автоматическая регистрация
- При первом запуске устройство регистрируется на backend
- Отправляются: Android ID, AAID, модель, версия

#### 2. Heartbeat
- Каждые 30 секунд отправляется heartbeat
- Backend отслеживает статус устройства

#### 3. Выполнение задач
- Задачи получаются с backend
- Поддерживаются: parsing, surfing, uniqueness

#### 4. Уникализация (Root)
- Регенерация Android ID и AAID
- Очистка данных Chrome/WebView
- Изменение User-Agent, timezone, GPS
- Модификация build.prop

## 🏗️ Архитектура

```
com.automation.agent/
├── MainActivity.kt              # Главная активность
├── services/
│   ├── ControllerService.kt     # Фоновый сервис (heartbeat, задачи)
│   ├── TaskExecutor.kt          # Выполнение задач
│   ├── TaskQueue.kt             # Очередь задач
│   └── UniquenessService.kt     # Уникализация устройства
├── browser/
│   ├── BrowserController.kt     # Интерфейс браузера
│   ├── WebViewController.kt     # WebView реализация
│   ├── ChromeController.kt      # Chrome реализация
│   └── BrowserSelector.kt       # Выбор браузера
├── automation/
│   ├── Navigator.kt             # Навигация и взаимодействие
│   ├── Parser.kt                # Парсинг данных
│   ├── Screenshot.kt            # Скриншоты
│   └── DataExtractor.kt         # Извлечение данных
├── scenarios/
│   ├── Scenario.kt              # Модели сценариев
│   ├── ScenarioParser.kt        # Парсер JSON
│   └── ScenarioExecutor.kt      # Исполнитель сценариев
├── network/
│   ├── ApiClient.kt             # HTTP клиент
│   └── ProxyManager.kt          # Управление прокси
└── utils/
    ├── RootUtils.kt             # Root операции
    ├── DeviceInfo.kt            # Информация об устройстве
    ├── MockLocationProvider.kt  # Mock GPS
    └── GeoLocationHelper.kt     # IP геолокация
```

## 🔌 API

### Backend Endpoints

```
POST /api/agent/register      # Регистрация устройства
POST /api/agent/heartbeat     # Heartbeat
GET  /api/agent/tasks         # Получение задач
POST /api/agent/tasks/:id/result  # Результат задачи
POST /api/agent/screenshot    # Загрузка скриншота
POST /api/agent/parsed-data   # Отправка данных
```

### Формат регистрации

```json
{
  "androidId": "abc123...",
  "aaid": "def456...",
  "model": "SM-G973F",
  "manufacturer": "samsung",
  "version": "13",
  "userAgent": "Mozilla/5.0...",
  "isRooted": true
}
```

## 📝 Сценарии

Сценарии хранятся в `assets/scenarios/` в формате JSON.

### Типы сценариев

1. **parsing** - Парсинг данных с веб-страниц
2. **surfing** - Имитация серфинга
3. **automation** - Автоматизация форм
4. **uniqueness** - Уникализация устройства

### Пример сценария парсинга

```json
{
  "id": "parse_ads",
  "name": "Парсинг рекламы",
  "type": "parsing",
  "browser": "webview",
  "steps": [
    {"type": "navigate", "url": "https://example.com"},
    {"type": "wait", "duration": 3000},
    {"type": "extract", "selector": "a[href*='adurl']", "save_as": "ads"},
    {"type": "screenshot", "save_as": "page"}
  ],
  "post_process": {
    "extract_adurl": true,
    "deduplicate_domains": true
  }
}
```

### Доступные шаги

| Тип | Описание | Параметры |
|-----|----------|-----------|
| navigate | Переход по URL | url, wait_for_load |
| wait | Ожидание | duration, random_offset |
| click | Клик | selector |
| input | Ввод текста | selector, value |
| scroll | Прокрутка | direction, pixels, percent |
| extract | Извлечение | selector, attribute, save_as |
| screenshot | Скриншот | full_page, save_as |

## 🔧 Troubleshooting

### Приложение не запускается

1. Проверьте версию Android (минимум 7.0)
2. Проверьте, что APK подписан
3. Проверьте логи: `adb logcat -s AutomationAgent`

### Нет подключения к backend

1. Проверьте URL сервера
2. Проверьте сетевое подключение
3. Проверьте настройки прокси

### Root функции не работают

1. Убедитесь, что root доступен: `adb shell su`
2. Предоставьте root права приложению
3. Проверьте, что Magisk/SuperSU установлен

### WebView не загружает страницы

1. Обновите Android System WebView
2. Проверьте сетевое подключение
3. Проверьте настройки прокси

### Логирование

```bash
# Все логи приложения
adb logcat | grep -E "(AutomationAgent|WebViewController|TaskExecutor)"

# Сохранить в файл
adb logcat -d > logs.txt
```

## 📄 Лицензия

Проприетарное ПО. Все права защищены.

## 📞 Поддержка

При возникновении проблем создайте issue с:
- Версией Android
- Моделью устройства
- Логами ошибки
- Шагами воспроизведения
