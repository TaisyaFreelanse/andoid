# MinIO на Render - Финальная конфигурация

## ✅ Решение проблемы с редиректами

Проблема с бесконечными редиректами в MinIO Console на Render возникает из-за особенностей проксирования портов. 

**Решение:** Отключена MinIO Console, используется только API.

## 📊 Текущая конфигурация

### MinIO API
- **URL:** `https://android-automation-minio.onrender.com`
- **Порт:** 9000 (проксируется автоматически через Render)
- **Access Key:** `minioadmin`
- **Secret Key:** `minioadmin123`
- **Bucket:** `screenshots` (уже создан)

### MinIO Console
- **Статус:** Отключена (чтобы избежать проблем с редиректами)
- **Причина:** На Render сложно настроить несколько портов без циклов редиректов

## 🔧 Управление MinIO

### Вариант 1: Через Backend API (рекомендуется)
Backend уже настроен и работает с MinIO:
- Загрузка файлов через `/api/artifacts/upload`
- Получение файлов через `/api/artifacts/:id`
- Все операции выполняются через backend

### Вариант 2: Через MinIO Client (mc)
Установите MinIO Client и подключитесь:

```bash
# Установка mc (если еще не установлен)
# Windows: choco install minio-client
# Linux/Mac: brew install minio-client

# Подключение к MinIO
mc alias set myminio https://android-automation-minio.onrender.com minioadmin minioadmin123

# Проверка подключения
mc admin info myminio

# Просмотр bucket
mc ls myminio/screenshots

# Загрузка файла
mc cp local-file.png myminio/screenshots/

# Скачивание файла
mc cp myminio/screenshots/file.png ./
```

### Вариант 3: Через MinIO API напрямую
Используйте MinIO SDK для вашего языка программирования.

## ✅ Проверка работы

### Через Backend
Backend уже работает с MinIO. Проверьте логи backend - должны быть сообщения о подключении к MinIO.

### Через API
```bash
# Проверка bucket
curl -X GET "https://android-automation-minio.onrender.com/screenshots" \
  -H "Authorization: AWS4-HMAC-SHA256 Credential=minioadmin/..."

# Или через backend
curl https://android-automation-backend.onrender.com/api/artifacts
```

## 📝 Важные замечания

1. **MinIO API работает отлично** - все операции выполняются успешно
2. **Bucket `screenshots` создан** - видно в логах успешные PUT, GET, HEAD запросы
3. **Backend подключен** - все файловые операции работают через backend
4. **Console не критична** - все можно делать через API или backend

## 🔒 Безопасность

**Важно:** После проверки работы измените пароли!

1. В Render Dashboard для `android-automation-minio`:
   - Обновите `MINIO_ROOT_PASSWORD` на более сложный пароль
2. В Render Dashboard для `android-automation-backend`:
   - Обновите `MINIO_SECRET_KEY` на тот же пароль

## 🎯 Итог

✅ **MinIO работает:**
- API доступен и функционирует
- Bucket создан и работает
- Backend успешно подключается
- Файлы загружаются и читаются

⚠️ **Console отключена:**
- Чтобы избежать проблем с редиректами на Render
- Все операции доступны через API или backend
- Можно использовать MinIO Client для управления

