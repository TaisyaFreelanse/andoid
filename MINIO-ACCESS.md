# Доступ к MinIO на Render

## ⚠️ Важно: Render и порты

На Render **нельзя** обращаться к портам напрямую через URL (например, `:9001`). Render проксирует все порты через основной URL.

## ✅ MinIO API работает

MinIO API доступен через основной URL:
- **URL:** `https://android-automation-minio.onrender.com`
- **Порт:** Автоматически проксируется (внутренний порт 9000)
- **Access Key:** `minioadmin`
- **Secret Key:** `minioadmin123`

Backend уже настроен и работает с MinIO API. Видно в логах:
- ✅ Bucket `screenshots` создан
- ✅ PUT, GET, HEAD запросы успешны
- ✅ Backend подключается к MinIO

## 🔧 MinIO Console (Web UI)

### Проблема
MinIO Console работает на порту 9001, но на Render нельзя обращаться к портам напрямую.

### Решение 1: Использовать переменную окружения (уже настроено)
Добавлена переменная `MINIO_BROWSER_REDIRECT_URL=https://android-automation-minio.onrender.com`

После перезапуска MinIO Console должна быть доступна через основной URL.

### Решение 2: Альтернативный доступ
Если Console все еще недоступна, можно:

1. **Использовать MinIO Client (mc):**
   ```bash
   mc alias set myminio https://android-automation-minio.onrender.com minioadmin minioadmin123
   mc admin info myminio
   ```

2. **Использовать MinIO через API:**
   - Все операции можно выполнять через API
   - Backend уже настроен для работы с MinIO

3. **Использовать внешний MinIO Console:**
   - Установить MinIO Console локально
   - Подключиться к удаленному MinIO серверу

## 📊 Текущий статус

✅ **MinIO работает:**
- API доступен: `https://android-automation-minio.onrender.com`
- Bucket `screenshots` создан и работает
- Backend успешно подключается к MinIO
- Файлы загружаются и читаются успешно

⚠️ **MinIO Console:**
- Настраивается для работы через основной URL
- После перезапуска должна быть доступна

## 🔍 Проверка работы

### Проверить через API:
```bash
curl https://android-automation-minio.onrender.com/screenshots
```

### Проверить через Backend:
Backend уже работает с MinIO. Проверьте логи backend - должны быть сообщения о подключении к MinIO.

## 📝 Примечания

- На Render все порты проксируются через основной URL
- MinIO API работает на порту 9000 (проксируется автоматически)
- MinIO Console работает на порту 9001 (требует настройки для доступа через основной URL)
- Persistent disk (10 GB) обеспечивает сохранность данных

